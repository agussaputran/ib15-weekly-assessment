function getFullname(firstName, lastName) {
  //process
  const fullName = `${firstName} ${lastName}`;

  // nilai balikan
  return fullName;
}

// call the function
console.log(getFullname("Yosias", "Ignatius"));
